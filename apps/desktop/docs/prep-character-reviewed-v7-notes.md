# Reviewed v7 运行旁注

不修改冻结输入，不重跑试次，不覆盖原始判分。完整批次后将原始成绩与人工复核分开报告。

## A1 Qwen第一对

JS：276.910秒，20调用，工具时间3.303秒；当前验收失败为daylight职业列表与戏法3/4。
工具：158.768秒，19调用，工具时间2.998秒；属性[9,15,15,18,11,9]不满足共用标准数组+种族+ASI；HP30，当前合同应32，属于明确配置错误。
工具组另两项需要单独复核，不能无条件称为模型错误：
- 卜筮术来自自动化包准确UUID，查询返回的可选“额外法师法术”明文包含它。现有验收仅按SRD5.1职业列表拒绝，输入没有清楚冻结TCE可选扩展是否启用，因此是合同/验收歧义。
- AC calc=mage，最终15，已有法师护甲条目；验收预期未施法AC12。是否允许默认法师护甲及其消耗需按任务合同审查，不直接视为字段错误。

这两项即使后续复核放行，工具组仍有属性和HP错误，不能变成成功样本。当前两组均失败，耗时差不能宣称有效提速。


## A1 DeepSeek第一对

工具组64.732秒/15调用，查询工具调用2次；属性[8,15,14,18,13,11]不满足标准数组+人类+ASI，力量应为9才与该分配相符，其余当前检查通过。JS组113.947秒/30调用，速度和法术书检查未通过。两模型A1都出现工具组更快，但四组均未完整通过，不能按成功耗时计算收益。当前只记录待后续题验证的趋势。


## A2 Qwen第一对

JS175.765秒/17调用：给五级角色导入七级运动健将，属性[16,15,14,11,13,9]，HP46/预期44，步速0/预期30。工具166.353秒/14调用：未再出现越级、HP、步速错误，但属性[20,15,14,13,11,9]不合法，运动/察觉的关联属性均为dex。只能记录错误类型减少与调用减少，两个失败样本不支持成功耗时收益。

## A2 DeepSeek工具组：首个完整通过样本

run 1788957371879，149.603秒/35调用，验收v4全部通过。查询调用1次，返回19599 bytes；工具总执行14.342秒。调用构成为read 4、foundry_open 1、world_status 1、foundry_build_query 1、browser_evaluate 16、powershell 12。

工具调用轨迹显示，查询之后仍多次探索NPC schema，反复读取本地dnd5e.mjs的熟练、等级、HP和种族逻辑；12次PowerShell主要用于定位、搜索和读取系统源码，并非反复找职业包。随后批量创建，单独修正运动/察觉关联属性，再读回检查。不能把剩余耗时全部归因于thinking；它包含模型生成、供应商等待、传输及工具往返。也不能从总耗时中精确推算各项占比。

后续候选方向是改进经过本地验证的NPC API操作指南，减少schema试错和派生值源码探索；本批输入保持冻结，不因首个通过样本临时改skill。它证明本题可完成，但是否优于裸JS须等待同题对照和其余案例。

同题JS组run 1788957530188完成：157.879秒/35调用，工具执行2.303秒，验收v4全部通过。此为首对双方成功的匹配样本，工具组少8.276秒（约5.2%），调用数未减少。每组只有一次，不支持稳定提速结论，也不支持查询工具对本题正确率必不可少的说法。

## A3 Qwen裸JS终态

run 1788957697900：272.345秒、25调用，工具执行2.520秒；正常返回，未超时。正式验收5项失败：步速0/预期25，洞悉/医药/宗教关联属性均dex，戏法4/预期3，等级法术8/预期10，普通准备法术4/预期6。尚未完成同题工具组，不作组间结论。步速与技能属性错误再次出现，值得在整批完成后合并归因。

A1/A2八份终态报告已逐一核对：300000ms、thinking high、character-v7-reviewed-growth，native/pack/build skill、查询工具及验收器hash全部匹配启动manifest。A1 DeepSeek JS的两项失败补充确切值：步速0/30，法术书10/14。

### 归因约束：已有指南不能算成缺失知识

复读冻结的native skill确认：已经明确要求技能ability取源值或CONFIG.DND5E.skills[key].ability，读回ability/value/total；共同defaults也已说明NPC熟练按max(CR,class level)派生。因此重复的技能字段错误及熟练源码探索，不能简单归为“没有告诉模型”。下一轮要核对模型实际读取、信息位置与执行/验收是否落实，再决定压缩重组说明还是补充缺项。步速在当前native字段表中没有专门条目；这与已提供但未执行的技能说明是不同问题。本批不修改这些输入。

## A3 Qwen工具组及对照

run 1788957979562：170.033秒/12调用、工具执行3.321秒，未超时，仍失败。比JS少102.312秒和13调用，但不能算成功提速。法术与步速检查通过；失败为子职关联、额外熟练等价条件、属性、HP、技能关联属性和轻锤。

原始卡及写入轨迹确认子职不是漏识别：模型明确把Life Domain的system.classIdentifier写成life-domain，应关联职业cleric；它也导致额外熟练的子职等价分支不能成立。这两项是同一关联错误的连带判分，不能计成两个独立根因。属性[10,12,17,8,16,13]不满足本题标准数组+丘陵矮人；HP32而预期30。

装备实际导入已装备的“残暴轻锤”，identifier=vicious-light-hammer、无compendiumSource。模型脚本用/light\\s?hammer|轻锤/i取首个匹配，并允许世界Item回退，没有精确选择普通轻锤。故该项不是完全没装备武器，而是选错普通起始装备。与职业成长查询的覆盖范围分开归因。

## A3 DeepSeek工具组

run 1788958159170：146.542秒/22调用、工具执行3.922秒，未超时。仅movement.walk与armor.effective未通过，其余正式检查通过。实际卡步速0/预期25；装备鳞甲(14、敏捷上限2)与盾牌(+2)，DEX12应AC17，却使用ac.calc=flat、flat=16。它不是验收忽略某种护甲表示，而是固定AC与已装备物品及属性不一致。同题JS仍在运行，暂不作对照结论。

同题JS run 1788958316022终态：223.235秒/46调用、工具执行8.025秒，全部通过，无超时。工具组少76.693秒、24调用，但未做对；此题不能列入成功耗时匹配对，也不能宣称工具提高正确率。A组三题共12试次完成，Qwen两臂均0/3；DeepSeek JS 2/3、工具1/3。样本量不足以判定稳定优劣，B组继续检验保留与增量合并。

## B1 Qwen裸JS：首个Qwen通过样本

run 1788958550242：130.132秒/17调用，工具执行0.436秒，全部正式检查通过，无超时。狼人追加五级冠军勇士的原有内容保留、CR保留、属性增量、HP和职业配置通过。它反驳“Qwen无论如何都无法完成”的强结论；已有来源作为基础可能减少从零配置错误，但需其余B组验证，不能由单题推出普遍规律。工具对照仍在运行。

同题工具run 1788958689502：99.754秒/11调用，工具执行0.402秒，全部通过，无超时。双方成功可配对：少30.378秒（约23.3%）、6调用，工具组进入120秒体验目标。本题提供正面单次证据，仍需复测才能判断稳定性；不抵消A组失败，也不推广到所有车卡任务。

## B1 DeepSeek与四试次收口

工具run 1788958798482：54.093秒/13调用、工具执行0.211秒，全部通过；JS run 1788958861488：120.233秒/26调用、工具执行0.429秒，全部通过。工具少66.140秒（55.0%）和13调用。工具卡HP90、CR3，六个原有条目内容均通过保留检查。

B1四试次全部通过，两模型的工具组均更快且调用更少。这是本轮最清晰的正面案例，但每种组合仍只有一次，不能估计重复运行方差或推广到从零车卡。累计16/24完成，7次通过、无超时；继续B2/B3。

## B2 Qwen对照：提前取整造成HP差1

JS run 1788958990636：139.764秒/17调用、工具执行0.843秒，全部通过。工具run 1788959139342：137.912秒/18调用、工具执行0.477秒，仅hp.full失败，实际16/预期17。均未超时。

工具组执行脚本明确写“d6 (mean floor=3)”并按7+3×3=16计算；共用skill要求只对最终总和取整，应floor(7+3×3.5)=17。不是源包缺数据或查询遗漏，而是没有遵守已提供的取整约束。两组耗时接近，工具组未完整通过，不算有效速度收益。累计18/24完成、8次通过，DeepSeek B2继续。

## B2 DeepSeek双方通过

工具run 1788959286859：105.582秒/23调用，工具执行1.887秒；JS run 1788959401721：157.506秒/29调用，工具执行12.519秒；均完整通过，无超时。工具少51.924秒（33.0%）、6调用并进入120秒。两组工具执行时间本身也有差异，不能将全部耗时差解释成thinking减少。累计20/24完成、10次通过，最后B3四次继续。

## B3 Qwen裸JS

run 1788959568663：158.604秒/14调用、工具执行0.866秒，未超时；仅spells.book失败，等级法术12/预期14，其余正式检查通过。法师法术书数量已经在共同skill中明确，后续归因为遗漏，不能自动视为查询缺项。工具对照继续。

同题工具run 1788959737268：159.371秒/14调用、工具执行0.656秒，全部通过，无超时。耗时与调用几乎相同，体现本次完成质量差异，不体现提速。Qwen六题两臂均2/6通过：JS通过B1/B2，工具通过B1/B3，不能只报相同总通过率而掩盖成功任务不同。

## B3 DeepSeek工具组

run 1788959905169：189.822秒/40调用，工具执行32.229秒，全部通过，无超时。这次工具组调用仍多，不能把所有任务都概括为查询后一步完成；同题JS仍待终态。

轨迹确认23次PowerShell（31.447秒）查本地dnd5e源码，涉及AdvancementManager、施法等级、NPC/Character派生、武器熟练、sourceClass、countsPrepared与生命骰模板；foundry_build_query只有1次。与A2 DeepSeek重复出现“来源已查到后继续探索实现”的现象。不能据此强制禁止模型验证，而应下一轮验证精简、经过本地核对的API说明能否减少无效探索。

同题JS run 1788960104314：112.033秒/26调用、工具执行1.182秒，全部通过，无超时。工具比JS多77.789秒和14调用，是双方成功但工具更慢的反例。24次全部结束，启动器退出0，最终完整结果与后续建议见[整批报告](prep-character-reviewed-v7-results.md)。本文件保留各阶段当时的旁注，不将此前“待运行”文字当成当前批次状态。
