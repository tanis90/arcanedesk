# 规则skill有无对照：三对法师任务

已完成。**本轮没有证明规则skill提升正确率；当前注入方式的限时完成表现更差。** 无规则3/3完成、有规则1/3完成；双方观察到的角色数据均3/3通过，其中有规则的两份在超时停止后复核。完整指标、会话文件名、来源hash和对象标识见[结构化记录](prep-rules-ablation-results.json)。本轮进度同步到[唯一技术方案第20节](foundry-prep-play-technical-plan.md#20-规则skill有无对照2026-09-09)。

| 配对 | 无规则skill：耗时 / 调用 / 轮次 | 有规则skill：耗时 / 调用 / 轮次 |
| --- | --- | --- |
| 1，无→有 | 117.60秒 / 8 / 7，通过 | 180.01秒 / 13 / 7，超时 |
| 2，有→无 | 154.05秒 / 14 / 10，通过 | 180.02秒 / 24 / 21，超时 |
| 3，无→有 | 151.54秒 / 9 / 8，通过 | 135.16秒 / 14 / 8，通过 |

无规则平均观察耗时141.06秒，有规则165.07秒；后者包含两次180秒截断，**不是成功任务平均完成时间**。平均调用10.33→17；120秒内正确完成1/3→0/3。第三对候选快16.38秒，所以不能说每次都会更慢。小样本不作稳定胜率或统计显著性声明。

2026-09-09。预注册配置：本地COS、qa-aliyun-token-plan / qwen3.7-plus，thinking开启（client high，实际enable_thinking=true，reasoning_effort省略），17个相同工具，180秒硬上限、120秒体验目标。

两臂均提供实验fvtt-native-npc指南及生产arcane-actor-update查包指南，仅候选额外提供arcane-dnd5e-rules目录与读取指令。每次fresh会话、独立命名对象，执行顺序：无/有、有/无、无/有，共6次。固定题目与既有9项数据验收，不因本轮输出新增/删除评分项。效果指整个规则skill的暴露与路由，不能分离索引措辞、原文或多读一次的影响。

主要指标：独立数据验收通过数、重复创建、法术位检查；辅助指标：正确且120秒内完成、所有任务耗时、成功任务耗时、调用次数、模型工具轮次、实际规则读取、工具错误。保持完整原始报告，超时/未决写入停止批次并先审查，不自动重放；NPC保留复核。此前thinking开关小测不混入本轮。

运行：`--comparison=rules-ablation --cases=npc_wizard --samples=3 --thinking=high --task-timeout-ms=180000`，其余连接参数见[benchmark说明](prep-benchmark.md)。原始报告：`benchmark-1788929938295.json`。本文件在结果出现前登记配置；现已补齐全部六次结果。

## 中断与后续独立任务

第一对：无资料117.60秒通过；有资料180.01秒超时，taskState=stopped，全部13次工具调用已终结。停止后用同一只读验收器复核，9项数据均通过、仅一份NPC，但任务未按时返回，超时失败计分不变。

没有重放或清理该任务。余下4次以新报告 `benchmark-1788930309463.json`、samples=2、reverse-first执行，顺序有/无、无/有，与原定剩余顺序一致。只因运行器在超时后停止而拆分报告，不补跑已完成/失败样本；其他配置与来源保持不变。

第二对的有资料任务再次180.02秒超时，taskState=stopped，24次工具调用全部终结；停止后的同一验收器检查9项通过、一个NPC，仍计超时失败。其余三次分别在 `benchmark-1788930631095.json`（第二对无资料）和 `benchmark-1788930803279.json`（第三对无/有）继续，不重放超时任务。第二对无资料154.05秒完成并通过。

两次超时复核原始数据分别保存在实验profile同级的 `rules-ablation-timeout-check.json`、`rules-ablation-timeout-check-2.json`；它们是停止后的额外观察，未改写原报告，不能证明模型在截止前已经自行完成验收。

## 轨迹观察与解释边界

第一条有资料任务实际读取索引及Wizard、Human、Fireball、Weapons；随后三次素材查询，生成6066字符的创建脚本，再进行HP修正，未在上限内给出最终回答。第二条有资料任务读取索引及Wizard、Human、Fireball，反复检索法术、职业特性和塑能学派；一次 `idx.slice is not a function` 后修正Collection用法，后续创建、修正HP/AC、添加条目和检查。存在明确的素材发现与API往返，不能把延迟一概归为阅读规则或thinking。

原有9项检查只覆盖当前题目的若干关键数据，包括4/3/2法术位、火球术活动结构、单一NPC和长棍；不验证实际施法、完整职业机制、CR合理性或全部规则。因此“数据全通过”不等于完整角色规则认证。保持既定评分，不在看见结果后加题改变本轮胜负；新增质量维度应另行定义后测试。

第三条候选读取与第一条相同的四份规则正文，完成创建、检查后尝试截图，收到 `Foundry panel cannot capture its viewport`。随后仍在135.16秒结束，原验收通过；截图错误保留，不把它隐藏为无错误样本，也不将它解释为规则理解错误。

六次工具执行累计各约0.36–1.14秒，等待主要在工具之间的模型请求与输出；不能将剩余墙钟时间全部称为thinking，供应商排队、网络、生成和调度未独立测量。三条候选均确实读取索引、Wizard/Human/Fireball正文，两条还读Weapons；三条对照均读NPC与查包指南，未读规则skill。无规则对照也全部正确得到4/3/2，因此本题没有观察到可被规则skill修复的该类错误。

固定项核对通过：17工具及其文件hash、runtime hash、NPC指南hash、查包指南hash、Foundry/system/modules一致；候选规则目录hash一致；所有请求thinking开启。normalization v2后，同臂三次system prompt hash一致。保留对象的世界、供应商缓存与延迟不是重置的实验环境；三对和顺序交错不足以消除这些因素。

## 迭代结论

保留知识资料的可查询能力，但不据此宣称默认注入已改善角色创建，也不扩大成每次创建都应读取整段职业规则的产品要求。下一步若验证知识价值，应预先补充有规则分歧或模型易错的任务及独立正确性标准，再比较是否找到依据、纠正错误且正确结束。若继续优化当前任务，优先查明素材反复定位、API误用和收尾扩展，避免用更多规则文字掩盖执行问题。本轮不再补跑、不修改skill来追逐这六个样本，也未修改auto pack或启动新goal。
