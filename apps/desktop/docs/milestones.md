# 多会话实施与验收记录

依据：[体验规格](spec.md)、[架构设计](architecture.md)。在当前 main 分支实施，每个 milestone 完成相应验收后独立提交。仅完成全部阶段及最终规格审计后才算完成整体目标。

| 阶段 | 范围 | 验收门槛 | 状态 |
| --- | --- | --- | --- |
| M1 | 主进程运行现场、带版本事件、快照及有界增量缓冲 | 流式、工具、重试现场可恢复；快照隔离；事件断档退回快照；接入 host 不破坏现有行为 | 已验收 |
| M2 | 会话注册表、任务身份、按会话命令与执行状态 | 同模式 A→B→A 不停止；迟到回执不串会话；准确停止指定任务 | 已验收（模块级） |
| M3 | 前端投影及恢复协议、草稿附件和阅读状态 | 快照事件竞态、前端刷新、半截回复与长工具、快速切换、阅读锚点 | 已验收（页面与协议） |
| M4 | 输入接收与幂等、任务结束竞态、结构化待处理 | 输入不丢不重复；接收与消费区分；回答正确归属 | M4a/M4b 已验收 |
| M5 | 跨模式活动、未读与通知 | 后台完成/失败/待处理可见；不抢焦点；恢复不重复通知 | M5a–M5d 实现及自动化验收完成；系统展示待 M8 平台审计 |
| M6 | 并发额度、共享资源协调、取消队列 | 独立任务并发；受控冲突串行；等待可见且可取消 | M6a/M6b、M6c1–M6c4 已验收；完整资源集成审计待 M8 |
| M7 | 持久任务记录、删除、退出与异常恢复 | 不产生孤儿任务；重启标中断；不重放外部动作 | M7a–M7e 自动化实现阶段已验收；系统入口实测及完整恢复体验待 M8 审计 |
| M8 | 完整集成验收、性能测量、规格逐项审计 | spec 全部场景有证据，缺失项保持未完成 | 已推进至 M8m3；G1–G5 已关闭，G6 部分完成，G7 及最终性能/规格复核待完成 |

## 验收记录

按阶段记录实际执行的检查、结果、限制及提交。自动化测试不能替代未覆盖的 Electron 交互与实际 SDK 行为验证。

### M1 — 2026-09-07

- 新增 SessionProjection：保留累计文字/思考、工具状态与起始时间、重试状态；完成消息移除流式副本；不把未结束工具伪造为成功。
- AgentHost 出站事件更新现场后发布；currentPayload 增加 inFlight。共享 world_info 保持既有全局数据边界。
- 新增 5 项测试，覆盖真实 host 转换、快照边界后的回放、断档与 epoch 不符时回退、所有数据出口的可变引用隔离。
- 验收：desktop typecheck 通过；desktop 全量 293 项测试通过；47 个 Markdown 文件链接检查通过。
- 限制：此阶段是运行现场基础，尚未接入前端补水；投影生命周期与按会话注册表在 M2 合并，前端订阅协议在 M3 接入。现有同模式切换仍会中止，不宣称用户问题已修复。

### M2 — 2026-09-07

- 每模式增加 SessionRegistry，每个 resident 会话保留独立 AgentHost；界面新建和打开会话不再调用原 host 的 abort/detach。目录在实例创建时固定，打开历史时恢复其目录。
- ModeHostController 快照捕获实际会话实例；命令上下文增加 sessionId/taskId；前端按会话、任务隔离事件和回执。停止旧任务不能作用于新任务。
- 任务执行状态从 busyByMode 移到各 host，运行开始和终止发出 task_state。SDK 的 agent_end/settled 不再直接清掉有任务身份的前端 busy。
- 验收：desktop typecheck 通过；全量 297 项测试通过；随后增加的 renderer 事件隔离测试及 registry 定向测试共 5 项通过。
- 测试使用真实 AgentHost/ModeHostController 和可控 session 替身，证明应用生命周期与路由；真实 SDK 并发、Electron 完整交互由 M4/M8 验证，不能将模块测试等同于产品终态验收。
- 后续：M3 接入恢复快照、导航竞态和阅读状态；M4 完善 SDK 停止/正常完成竞态、输入接收及任务协调器抽离；M5 需要把现有按模式统计的 telemetry 上下文改成按会话隔离；M7 完善删除准入锁、实例回收和退出恢复。

### M3 — 2026-09-07

- 快照包含运行现场并接入全部恢复入口；恢复时重放已接收但晚于快照的事件，重复事件不渲染，断档重新取快照，退役进程的迟到事件不污染新缓存。
- 导航请求使用最后选择生效；新建/打开由对应响应安装快照。后台消息仍更新事件缓存，返回可先展示缓存现场。同步失败保留已确认内容并提供重试入口。
- 运行工具保留起始时间和运行态，无结果的历史调用显示未确认；终稿替换半截内容。每会话草稿、附件、阅读锚点及展开状态保存在 IndexedDB，异步图片处理绑定原会话。
- 验收：desktop 全量 302 项测试通过；随后新增退役 epoch 测试，前端定向 5 项通过；最终 typecheck、source boundary 和文档链接检查通过。
- 真实 Electron 验收命令：`node apps/desktop/test/smoke-conversations.mjs`。使用真实 HTML、preload、渲染代码和 IndexedDB，隔离 userData、模拟主进程任务数据、不调用外部模型。覆盖 A→B→A、工具原始时间、半截恢复、草稿和附件隔离、阅读锚点、展开状态、刷新后恢复、后台完成且无重复终稿。
- 限制：真实 SDK 事件与工具副作用验证仍归 M4/M8；长历史分页、跨进程持久消息身份、缓存回收及性能基准在最终集成审计中继续核对。页面验收不等于全部 spec 已完成。

### M4a — 输入登记与执行协调 — 2026-09-07

- 抽出 TaskCoordinator 与 InputJournal。输入持久登记后即回执；commandId 与内容摘要保证重复回执不重复执行，变更同一命令内容会拒绝。附件准备发生在幂等检查之后。
- 协调器管理任务与待处理输入，执行中的补充通过 SDK steer 入队；结束边界未被消费的输入由同一任务接着处理。停止先清队列；旧任务停止命令不影响后续任务。
- 真实 SDK 实验发现：停止时 user message_start 可以发生而没有后续模型调用。因此区分“进入上下文”和“助手流开始”，只有后者标记已交给助手；不将入队或写入历史当成已消费。
- 前端保存带 commandId 的 outbox，捕获会话目标后才调用 IPC；失去回执时提供同 ID 重试。发送状态和消费状态分别展示。恢复日志标记未终止任务为 interrupted，不自动重放。
- 验收：全量 desktop 311 项测试通过；随后增加磁盘写入失败测试，协调器定向 7 项通过。真实 Pi SDK 测试 2 项通过，分别验证补充在下一模型边界只消费一次、停止清除未消费队列。测试 provider 为进程内受控流，不连接外部服务。
- Electron 页面验收通过，增加“回执不确定→重试仍用同一 commandId”检查；最终 typecheck 与源码边界检查通过。
- M4b 仍需实现结构化提问/回答、待处理项恢复和任务内模型变更延后。完整任务恢复、日志压缩及失败后继续交互仍在 M7/M8 核对，M4 整体尚未完成。

### M4b — 结构化提问与任务模型边界 — 2026-09-07

- 两模式提供 request_user_input 工具；协调器持久记录问题、回答与状态，任务等待回答时仍可停止。回答同时校验会话路由、taskId、attentionId 和 commandId，重复回答复用回执，过期问题拒绝回答。
- 当前执行进程存活时，快照恢复问题卡片和回答草稿；进程重启后未解决问题标记 interrupted，不假装保留了原工具等待栈。恢复时先安装问题与输入状态，再重放新事件，避免把已回答状态倒退回 pending。
- 前端显示明确任务状态、选项与自由文本回答入口；回答尝试身份保存在工作区。已有可选写操作确认加入 waiting_user 状态及快照，停止会拒绝并解除其等待。
- 运行中的模型选择持久记录为下一任务配置，当前任务在接收时捕获模型变更边界；即使模型调用尚未启动，也不会被随后选择的模型改写。界面标明“下一任务使用”。
- 验收：desktop 全量 317 项测试通过；随后新增可选审批运行态测试 1 项通过；最终 typecheck、source boundary、Markdown 链接检查通过。
- 真实 Pi SDK 3 项测试通过，新增“真实工具等待回答→携回答进入下一模型调用→原 taskId 完成”。Electron 页面验收新增跨模式恢复问题和回答草稿、回答归属正确，并继续通过已有现场恢复/重试场景。
- 后续仍按 M5–M8 推进跨会话活动、资源调度、生命周期及完整规格审计，不将本阶段验收等同于整个终态已完成。

### M5a — 活动摘要与持久阅读边界 — 2026-09-07

- 新增 ActivityCenter，只派生任务摘要，不修改执行状态。按会话保留未读、阅读游标、待处理项及通知消费键；同模式和跨模式任务互相隔离，普通输出不重排列表。
- 主进程先发正文事件再更新活动摘要；高频输出合并发布，活动流使用独立 epoch/seq。会话列表带活动摘要，新增受信 IPC 快照和已读确认入口。
- 已读确认要求窗口在前台可见、阅读位置在底部，并以页面实际安装的正文游标校验；后台、历史位置及迟到确认不能清掉更新的结果。已读不清除完成或失败状态。
- 通知消费先持久化后派发；问题和现有审批按具体项去重，等待状态本身不重复提醒。恢复快照只校准摘要，不重新派发历史提示。写入失败保留内存活动与错误状态，抑制未持久确认的通知。
- 验收：新增 7 项活动模块测试通过，desktop 全量 325 项测试通过，typecheck/source boundary 通过，原有 Electron 会话恢复与问答验收通过。新增测试覆盖同模式并发、阅读竞态、通知持久去重、重启校准、500 个流片段合并、磁盘故障和摘要不含正文/工具参数。
- 限制：这是主进程及 IPC 基础验收。跨模式活动界面、阅读分界、系统通知授权与点击策略、遥测会话隔离仍待 M5 后续实施；尚不能宣称后台结果已经对用户可见。日志压缩和生命周期的一致性继续在 M7/M8 核对。

### M5b — 活动导航及阅读体验 — 2026-09-07

- 前端新增 ActivityView，接入活动全局快照、增量和断档恢复。顶部活动栏始终显示运行与待处理数量，宽聊天区域常驻侧栏，窄区域使用可关闭且不参与隐藏键盘焦点的抽屉。
- 活动项与模式内会话项分别展示选中、状态及未读；流式更新复用活动行，不反复排序。活动点击跨模式进入目标会话，问题提示可直达对应问题卡片。
- 完成、失败、待处理提示显示在顶部活动栏，保留当前输入焦点，不遮挡输入区。多条提示合并计数，可收起；恢复页面只恢复摘要，不重弹旧提示。
- 已读确认绑定已安装现场、可见前台及实际到达底部。返回历史位置保留锚点，展示阅读分界及“有新进展”；读完只清未读，任务终态保留。修复仅有问题卡片时欢迎页未隐藏的问题，统一问题输入样式。
- 验收：desktop 全量 325 项测试、typecheck/source boundary、原有 Electron 会话恢复验收通过。新增 `node apps/desktop/test/smoke-activity.mjs`，真实 HTML/preload/IndexedDB 与 ActivityCenter，模拟执行和前台判定，隔离 userData，不调用外部模型或打开可见测试窗口。
- 新页面验收覆盖后台完成不抢 B 的焦点/草稿、返回 A 历史位置仍未读、到达最新才已读且保留 completed、跨模式直达问题、宽窄导航、刷新去重、活动事件断档补齐。使用 Electron offscreen 实际绘制并检查宽/窄与亮/暗截图；普通隐藏窗口捕获可能缺失合成层，未将该类截图当作产品渲染证据。
- 限制：系统通知启用设置、系统通知点击恢复及同模式并发遥测隔离仍待后续 M5；删除竞态、实例回收及资源协调继续按 M6–M8 完成。

### M5c — 会话遥测隔离 — 2026-09-07

- 每个 resident AgentHost 持有独立遥测上下文，隔离活动回合、重试、工具计时、技能读取与摘要。所有会话仍共享唯一 writer、uploader、授权状态、安装身份和递增事件序号，不增加上传实例。
- 会话与回合继续使用本次启动内的随机关联 ID；实际会话/任务 ID 不进入事件。主进程接收输入后向目标 host 记录，任务终态由 TaskCoordinator 驱动遥测结算；SDK 片段结束和点击停止本身不提前结算。
- 共享 Foundry runtime 以异步调用上下文携带发起会话与模式，队列内等待不改变归属。无会话的原调用继续走全局观察入口；不改变 runtime 执行及错误语义。
- 关闭授权清空全部会话的统计状态，再次开启重新发布各会话的模型维度，跳过中途任务以免把半段记录当完整结果。释放会话回收其统计上下文，不关闭共享 writer。
- 验收：新增 4 项测试，覆盖同模式相同工具 ID 的不同耗时、不同重试、独立摘要、统一事件序号、授权切换、释放隔离和共享 Foundry 队列归属。desktop 全量 329 项测试、typecheck/source boundary 及活动 Electron 验收通过；真实 Pi SDK 的输入/停止/提问回归 3 项通过。
- 后续：M5 尚需系统通知设置、派发策略与点击恢复；M6–M8 的调度、生命周期及终态审计仍未完成。

### M5d — 系统通知策略与点击返回 — 2026-09-07

- 通用设置新增默认关闭的系统通知开关，写入成功后生效。启用后仅在窗口不在前台时派发完成、失败或待处理通知，标题为会话名，正文仅为状态标签，默认静音。
- 消费 ActivityCenter 已持久去重的新提示，不补发关闭期间或恢复历史的提示。已读完成、已回答问题、删除会话及关闭开关时撤掉对应通知；过期任务的通知点击不能跳转到新任务。
- 仅显式点击通知才显示/聚焦窗口。目标先留在主进程，页面就绪后一次性领取；页面重建丢失即时信号时仍可返回目标。通知派发或设置存储失败不改变任务执行。
- 验收：新增 4 项通知策略测试通过；desktop 全量 333 项测试、typecheck/source boundary 通过。Electron 活动验收扩展实际设置 IPC、点击返回及点击后页面重建恢复，继续通过既有跨模式/未读/草稿场景。
- 边界：测试使用原生通知替身，不在用户系统弹出测试通知。已验证策略、Electron 页面与 IPC 链路，尚未证明各平台系统通知中心实际展示；系统展示、平台限制及退出/重开入口继续纳入 M7/M8 审计，不将替身测试等同于原生系统展示验收。

### M6a — 执行额度与可取消排队 — 2026-09-07

- 两模式共享 FIFO ExecutionScheduler，默认同时推进 2 个任务；`ARCANE_TASK_CONCURRENCY` 可设为 1–16 的整数，无效配置退回 2。选择会话不改变队列顺序。
- 任务接收后登记排队状态，获得额度才进入执行。未开始任务取消后显示 cancelled，保留输入取消状态且不会启动；已执行任务收到停止请求后，直到实际 prompt 退出才归还额度。
- 结构化提问及已有可选确认释放执行额度，回答持久接收后重新排队，获取额度后才恢复工具调用栈。多个待答项共享恢复闸门，不重复占位。
- 核对安装的 Pi agent-loop 与 tool-definition-wrapper：工具默认可并行，批次含 sequential 工具时顺序执行。生产提问和可等待确认的工具已有 sequential 标记，本阶段保留并据此验证释放额度不会让同批后续工具越过等待点。
- 验收：新增 4 项执行调度测试与 2 项真实 Pi SDK 测试；desktop 全量 339 项测试通过，typecheck/source boundary 通过。调度测试覆盖双任务占位、FIFO、取消、幂等释放、多问题恢复及停止后延迟归还；SDK 测试覆盖回答后排队不运行后续工具/模型，以及恢复排队中停止。已有可选确认测试增加额度释放检查并通过。
- 活动 Electron 验收继续通过，并增加新任务 queued 事件切换 taskId、排队文案及 cancelled 终态检查。SDK provider 为进程内受控流，不访问外部模型。
- 限制：执行额度不是资源锁。同工作区 shell/文件、Foundry 页面导航/截图/脚本的冲突协调仍待 M6 后续；暂不宣称已完成受控资源隔离或全量并发性能验收。

### M6b — 工作区资源协调 — 2026-09-07

- 两模式共享 ResourceCoordinator，以原子方式申请多个资源；冲突请求按先后顺序推进，无关工作区可以越过被阻塞的请求。等待可取消，取消排队不会启动对应工具。
- 包装 SDK 原生 read/write/edit 与既有 Arcane shell 工具，保留原参数、输出和环境配置。同工作目录及父子目录保守串行；路径按实际祖先目录解析，识别符号链接别名及尚未创建的目标。文件参数兼容当前 SDK 的 @、主目录、file URL、Windows shell 路径与读取名称变体；获准后再次核对路径，避免排队期间链接变化造成错误放行。
- 等待状态包含资源和占用任务身份，进入快照与活动摘要；当前任务显示等待的目录和占用会话。获得资源、取消或失败后清理对应等待记录，旧任务回调不能改写新任务。
- 验收：desktop 全量 347 项测试通过；随后增加路径拼写兼容测试，资源协调定向共 9 项通过。测试覆盖冲突 FIFO、独立目录并发、多资源无死锁、排队取消、回调重入、排队后路径复核，以及真实 SDK 文件写入和 shell 包装器。写入在 AbortSignal 后仍等待实际文件操作结束再归还资源；shell 异常后放行文件写入。
- Electron 活动验收增加资源名、占用会话和终态恢复检查并通过；原会话恢复、草稿、幂等重试及问答验收继续通过。类型与源码边界检查通过。
- 边界：shell 按工作目录保守协调，不能推断任意跨目录、网络或外部服务副作用。原生 shell 等待直接进程退出；脱离进程、外部程序及人工修改不在本阶段保证范围。文件路径兼容依据已安装 Pi 0.84.3，SDK 升级需回归。Foundry 页面和运行时调用仍待 M6c，完整生命周期及规格审计仍待 M7/M8。

### M6c1 — 任务侧 Foundry 协调与底层执行寿命 — 2026-09-07

- 任务的打开页面、截图、任意脚本及结构化 runtime 调用共用页面资源准入。申请资源前处理脚本确认；获得资源后重新获取当前页面，排队期间更换页面不使用旧引用。等待文案明确显示 Foundry 及占用任务。
- ResourceCoordinator 支持将租约保留到原始操作结束，工具的超时/取消回执不提前释放。通过异步上下文贯穿 SDK transport，页面读取、脚本和 GPU 截图跟踪原始 Promise；导航请求本身不足以解除占用，需等原操作结束且无待完成导航，或主页面导航提交、页面销毁、渲染进程退出。
- 导航失败且脚本仍运行时继续保留租约；正常结束清理页面事件监听。脚本发起导航的回执只描述请求已发出，不声称旧上下文已经释放。外部服务已经接收的写操作、脚本自行启动而未 await 的后台动作仍不能由页面 Promise 证明完成。
- 验收：desktop 全量 353 项测试通过；随后新增 GPU 超时寿命测试，页面、截图及资源定向共 24 项通过。覆盖超时后排队、运行中取消与排队取消隔离、导航请求/提交/失败、页面销毁/渲染退出、获取最新页面及结构化工具共用资源。测试夹具初次缺少 sendToRenderer，修复后定向与全量通过。
- 新增 `node apps/desktop/test/smoke-foundry-resources.mjs`，真实隐藏 Electron 页面执行受控异步脚本：超时后 B 不执行，A 实际写入后 B 才读取；实际页面导航释放旧页面未结束 Promise 的占用。隔离 userData，不连接 Foundry 服务或模型。类型与源码边界检查通过。
- 未完成：主窗口面板打开/关闭/刷新仍需统一协调，并提供长期未退出操作的恢复交互；任务停止状态需要等这些底层操作收尾再确认终态。真实 Foundry SDK 世界操作与这些入口的集成验收继续推进，M6 不因此标为整体完成。

### M6c2 — 任务终态等待实际操作收尾 — 2026-09-07

- ResourceCoordinator 按 sessionId/taskId 等待实际持有资源的操作结束，覆盖工具已经返回而原始页面 Promise 尚未完成的情况。其他任务的资源不阻塞本任务结算；状态观察回调失败也先等待释放再传播错误。
- AgentHost 将收尾接入 TaskCoordinator。每轮 prompt 返回及异常路径先等待底层操作，随后才能确认任务终态并归还执行额度。停止过程中保持 stopping；正常收尾等待显示本任务的资源操作仍未退出。收尾期间收到的补充输入继续由同一任务处理。
- 验收：desktop 全量 357 项测试通过；随后补充观察回调失败检查，任务、资源及页面定向共 32 项通过。包含真实 AgentHost 的停止链路：页面脚本收到取消后 task 仍 stopping、新输入拒绝、底层 Promise 完成后才 stopped。另验证执行额度保留、收尾期间补充不丢及跨会话结算隔离。
- 真实 Electron Foundry 资源寿命验收再次通过；类型、源码边界和文档链接检查通过。
- 后续：面板打开/关闭/刷新入口协调和长期未退出操作的恢复入口仍待 M6c3；删除与退出需要复用该收尾协议，继续在 M7/M8 实施和审计。

### M6c3 — 面板窗口操作排队与恢复显示 — 2026-09-07

- 新增 PanelCommands，主窗口打开、关闭、F5 刷新共用任务侧页面资源队列。请求立即返回身份和状态；重复点击合并为当前操作，排队可按命令身份取消，已经开始的导航不冒充可取消。刷新等待页面加载和检查后才显示完成，失败保留原因。
- 新增独立面板状态条，显示操作、占用会话、取消等待及终态。状态保留在主进程，聊天页面重建通过快照恢复；版本号避免迟到快照覆盖新事件。状态条跨任务显示，不把面板操作结果写进当前选中任务的聊天记录。
- 面板操作与状态/取消 IPC 校验受信聊天来源。任务和面板操作互相遵守资源占用，窗口关闭不越过执行中的页面脚本。
- desktop 全量 360 项测试通过，47 个 Markdown 文件链接检查通过。
- 验收：新增 2 项模块测试覆盖资源互斥、重复合并、取消不导航、迟到取消拒绝、加载结束后放行任务、加载失败及快照隔离。Electron 活动验收增加等待原因、聊天刷新恢复原请求、取消后导航次数为零、再次操作完成；原会话恢复、输入幂等和问答验收继续通过。类型和源码边界检查通过。
- 后续：当前普通面板操作保守等待；长期未退出页面操作的强制恢复尚未提供，需要协调受影响任务停止、旧页面销毁及新页面准入，不能以直接绕过锁的刷新按钮代替。该恢复交互和完整资源集成审计继续推进，M6 整体仍未结束。

### M6c4 — 旧页面恢复与准入屏障 — 2026-09-07

- 面板等待或失败状态新增重置入口，经明确确认后恢复页面。说明受影响任务将停止、已发出的世界写入可能已生效，不撤销、不自动重试，恢复后需核对世界状态。确认默认选择继续等待；IPC 校验受信聊天来源并合并重复恢复。
- desktop 全量 363 项测试、类型及源码边界检查、47 个 Markdown 文件链接检查通过。
- 仅允许恢复已经返回工具结果、但底层页面操作仍未退出的资源；普通工具调用尚在执行时返回明确等待原因，避免宿主调用栈仍操纵旧页面或随后修改新页面。确认后重新检查资源状态，不依赖弹窗前的判断。
- 恢复先设置页面准入屏障，取消尚未执行的面板命令，向持有旧页面的任务发送停止请求，再销毁旧 WebContents。等原资源实际解除后才重建页面，重建结束再放行后续请求。无关任务不收到停止请求；无法发出停止或销毁/重建失败时显示失败，原始未结束资源仍保留。
- 新增 3 项模块测试，覆盖活动工具拒绝恢复、恢复期间后续任务被拦住、排队面板请求取消、受影响 owner 身份、销毁与重建顺序，以及失败不释放旧资源。真实 Electron 验收新增销毁挂起页面并创建新页面；测试窗口关闭时显式保持测试进程存活，避免最后一个窗口关闭导致测试提前退出。
- Electron 活动界面验收覆盖恢复按钮、模拟确认取消保持原请求、确认后重建完成及导航计数。原生确认弹窗的人工点击没有用自动化替身冒充验证；主进程使用 Electron 标准对话框，真实系统展示仍归 M8 平台核对。
- 限制：正常调用须先返回或到达工具超时才可重置；不承诺撤销服务端已收到的动作。M6 自动化实现阶段完成，真实 Foundry 世界操作及最终规格逐项集成仍待 M8；继续推进 M7 删除、退出、恢复和实例回收。

### M7a — 删除准入锁与加载竞态 — 2026-09-07

- SessionRegistry 按规范化路径管理删除操作，重复请求合并。删除开始后 AgentHost 拒绝新输入，打开入口拒绝删除中和已删除路径；正在加载的历史会话在安装前复核，不会在文件删除后重新注册实例。
- desktop 全量 366 项测试通过；其后新增替代会话启动失败测试并通过定向回归；47 个 Markdown 文件链接检查通过。
- 等任务停止及底层操作收尾后才 unlink 会话文件，再释放 host；删除期间切到另一会话不被切回。磁盘删除失败保留实例并解除准入锁，可再次尝试。记录已删除后新会话初始化失败返回删除成功及附加警告，避免误报记录尚在。
- 删除确认说明运行任务会先停止并等待操作结束；按钮在请求期间禁用并标记停止/删除中，IPC 失败恢复按钮。列表恢复删除状态，历史打开失败转成可处理的结构化响应。
- 定向 9 项注册表/路由测试通过，新增使用真实临时文件的运行中删除、并发输入/打开拒绝、重复删除、加载中删除、磁盘失败及替代会话启动失败检查。类型和源码边界检查通过，原 Electron 活动及恢复验收继续通过。
- 范围：本阶段完成会话记录删除的准入和停止边界；持久任务日志、附件及前端工作区缓存的关联清理尚待后续，删除 UX 的完整 Electron 链路也继续纳入 M7/M8。退出、后台驻留和 host 回收仍未完成，不把本阶段当作 M7 整体验收。

### M7b1 — 前端工作区清理与迟到写入屏障 — 2026-09-07

- 删除成功或收到 activity_removed 时，移除会话快照、事件缓存、待重试输入、阅读恢复标记和对应回答草稿；当前视图仍指向被删会话时清空内容和输入，并使尚未安装的快照失效。已删除会话的迟到事件与快照不能重新创建前端记录。
- desktop 全量 368 项测试通过；随后补充未加载会话也派发清理事件的检查，活动模块 8 项测试通过；47 个 Markdown 文件链接检查通过。
- IndexedDB 工作区使用仅含删除标记的记录替换原草稿、图片、outbox、回答和阅读状态；内存立即封锁，写事务再次检查持久删除标记。并发保存或新存储实例收到旧保存请求都不能覆写删除标记。即使没有活动摘要，主进程也派发已完成删除的清理事件。
- 新增事件缓存删除屏障测试。真实 Electron 活动验收增加草稿/图片/outbox 清理、旧快照拒绝、并发保存/删除、新 WorkspaceStore 实例迟到保存不复活等 IndexedDB 检查；原会话恢复与问答验收继续通过。类型及源码边界检查通过。
- 附件边界：聊天草稿图片随工作区清理；项目 `.arcane/inbox` 中的文件已交给工具使用，可能被世界头像或素材引用，按项目产物保留，不能根据会话删除推断它们已经无人使用。
- 后续：主进程持久删除登记、任务日志清理和页面离线时错过删除事件的启动校准仍待 M7b2；IndexedDB 写入失败当前只有本次进程内屏障，尚不能声称跨进程关联清理完整。退出、后台驻留、实例回收及完整规格审计继续推进。

### M7b2 — 持久删除登记与启动补清理 — 2026-09-07

- 新增 SessionDeletions，使用 fsync 的追加日志登记删除意图、记录移除和关联清理完成。注册表等待任务停止后先登记，再 unlink 历史；登记失败不删除历史。完成后只清理由已验证会话 ID 派生的 tasks/<id>.jsonl，不接受任意任务文件路径。
- desktop 全量 375 项测试通过；随后强化记录类型和文件缺失判断后，6 项删除登记测试再次通过；47 个 Markdown 文件链接检查通过。
- 启动重放删除登记：历史仍在则取消未完成意图；历史已不存在则补记删除并清理任务日志。权限错误不当成文件不存在。清理失败保留已提交的删除身份供下一次启动重试；损坏记录阻止新增删除，不静默丢弃已登记内容。
- 主进程清理残留活动摘要并提供受信删除清单 IPC。前端在初始会话安装前及再次聚焦时据清单补清 IndexedDB 和内存状态，覆盖页面未收到删除事件的情况。只传会话 ID，不传历史或任务正文。
- 新增 6 项真实临时文件测试覆盖注册表实际删除边界、删除前/后退出恢复、清理失败重试、不同会话日志隔离、损坏身份和登记失败。Electron 活动验收增加未派发删除事件的页面重开补清理；类型与源码边界检查通过。
- 仍需完成退出/后台驻留、日志压缩和实例回收。删除清单本身保留最小身份标记以阻止旧缓存复活，其有界保留与迁移策略在后续审计中核对；现有项目 inbox 文件作为项目素材保留。

### M7c — 关闭窗口、后台入口及正常退出协议 — 2026-09-07

- 有任务或受控页面操作时，窗口关闭先提供取消、停止任务并退出；仅成功建立托盘重新打开入口后增加后台继续。后台选择隐藏原窗口，保留任务和 Foundry 页面。托盘可打开窗口或明确退出；明确退出对话框不提供后台选项。
- desktop 全量 379 项测试及 47 个 Markdown 文件链接检查通过。
- ShutdownCoordinator 关闭新任务、会话加载及面板命令准入，等待正在加载的实例退出准入流程，向活动任务发送停止，随后等待面板资源和删除操作收尾。确认结束后才释放 host 并退出；系统强制结束仍按既有持久日志恢复为中断，不重放任务。
- 页面显示剩余任务和操作收尾状态，可取消退出；取消不撤销已发出的停止请求，也不会被迟到完成回调重新退出。失败保留窗口并解除新工作准入。聊天页面重建通过 IPC 恢复退出进度。
- 新增 4 项协调器测试，覆盖全部任务和资源结算后退出、取消后迟到停止不退出及后续新任务、停止失败、加载中实例退出准入。Electron 活动验收增加退出进度、聊天刷新恢复、取消退出及迟到停止；原会话恢复验收继续通过。类型及源码边界检查通过。
- 平台边界：主进程已接入 Electron close/before-quit/activate 与 Tray；实际系统托盘展示、原生对话框人工交互和各平台关闭行为仍待 M8 实测。测试使用受控退出回调，未关闭用户正在使用的应用；不将组件验收等同于系统级后台驻留验收。
- 后续继续日志压缩、实例回收和中断后恢复交互核对，M7/M8 整体尚未完成。

### M7d — 任务检查点与删除日志压缩 — 2026-09-07

- InputJournal 增加完整副本写入、fsync 后原子替换；序列化或写入失败不替换原日志。成功压缩后继续追加，并可按同一恢复入口重新读取。
- desktop 全量 384 项测试及 47 个 Markdown 文件链接检查通过。
- TaskCoordinator 在任务非活动状态且积累 128 条记录，或存在已处理图片副本时生成 v1 检查点。保留每个命令的指纹和回执、输入最终状态、问题和回答、当前任务及下一任务模型；恢复后同 ID 重试仍返回原回执，变更内容仍拒绝。
- 清除已消费/已处理输入的 images、executionText 和 expandedText 冗余副本；未消费的失败、取消或中断输入保留附件与执行上下文供显式恢复，不把“任务结束”等同于“附件已被消费”。压缩失败只记录维护错误，不将已完成任务改成失败，也不丢幂等索引。
- 删除日志在重复或已取消意图积累后压缩，保留有效删除身份和未完成清理状态。不会回收仍用于跨进程前端清理的删除标记。
- 新增 5 项测试覆盖原子替换失败、替换后追加、输入及回答幂等、已处理大图片缩减、未消费中断附件保留、压缩失败不改变任务终态，以及删除身份恢复。原 Electron 会话恢复和问答验收继续通过；类型及源码边界检查通过。
- 范围：消除冗余状态记录，不截断命令幂等身份或用户历史；日志仍随真实输入数量增长。检查点规模、主进程写入延迟及最终保留策略继续在 M8 性能审计中核对；实例回收和完整终态审计仍待完成。

### M7e — 后台空闲实例回收与按身份重载 — 2026-09-07

- desktop 全量 387 项测试通过；最终类型、源码边界及 47 个 Markdown 文件链接检查通过。
- 每分钟检查一次各模式注册表。仅回收超过五分钟未使用、可安全持久化的后台实例；每模式保留最近四个合格空闲实例及当前选中实例。运行、等待用户、停止中、手动压缩、模型操作、删除中及尚未持久化的模型选择均保留，退出期间不回收。
- 核对安装 SDK：没有助手消息的会话可能尚未写文件。回收前使用公开 getHeader/getEntries 保存完整原生 JSONL，再释放实例；不伪造助手消息。写盘失败保留原实例。释放过程即使退订异常也尝试清理 SDK 与遥测，退役实例不再发布迟到事件。
- 注册表保留轻量 ID→路径定位，快照和按会话发送的命令可以重新载入实例，并保留当前导航。用户主动打开则沿用原导航协议。最近访问和事件更新使用时间，避免正在交互的后台目标被当成旧缓存。
- 新增 3 项原生 SDK SessionManager 测试，覆盖空会话/仅用户消息持久化、相同 ID 和历史恢复、原任务回执幂等、重载后可追加且只有一个会话头、退役事件抑制、回收例外与默认保留数量。类型及源码边界检查通过，既有 Electron 活动/恢复验收继续通过。
- 范围：这是可回收空闲实例的软上限，活动任务和未持久化状态不以省内存为由丢弃。前端历史缓存、消息稳定身份/分页、真实内存与延迟指标，以及操作系统入口的实际行为仍需 M8 全量规格审计；整体目标尚未达到。

### M8a — 审计修复：回收重载后的同步身份 — 2026-09-07

- desktop 全量 390 项测试及 47 个 Markdown 文件链接检查通过。
- 审计发现 M7e 与旧同步实现之间的集成缺口：SessionProjection 的 epoch 原先为进程级常量，但重建 host 会把 seq 清零。旧前端事件和活动游标因此可能把新实例事件视作重复，或将旧实例的高序号事件接到新现场。现在每个投影实例生成独立 epoch。
- EventInbox 明确接纳快照边界。缓存快照不能宣布替换已有 epoch；新的权威请求只有在发起时观察到的 epoch 未被其他实例超越时才能替换，替换后退役旧事件。新实例尚未发出事件时也能安装快照，避免一直等待不存在的事件来完成校准。请求被新实例事件超越时最多重新取三次，持续不稳定则显示同步异常。
- 退役事件直接在渲染入口丢弃，不能因缓存拒收后仍继续进入正文或触发重复同步。新增 3 项测试覆盖同进程游标不复用、无事件快照换代、旧事件/旧快照拒绝、请求与实例换代竞态。
- 真实 Electron 会话验收新增旧 seq=5 到新 epoch/seq=0、无新事件恢复、旧 seq=100 不污染正文及新 seq=1 正常显示；原草稿、阅读锚点、问答与重试验收仍通过。活动区 Electron 验收、类型及源码边界检查通过。
- 本轮修复的是同步边界，不等于完整 M8 已验收。消息持久身份、长历史性能、缓存容量和操作系统实际行为继续核对。

### M8b — 审计修复：消息身份与压缩后的完整历史 — 2026-09-07

- desktop 全量 393 项测试、类型与源码边界检查，以及 47 个 Markdown 文件链接检查通过。
- 审计发现消息使用 role:timestamp 作为流式去重和阅读定位身份，同毫秒的助手消息可能互相覆盖；buildHistory 使用 SDK 当前模型上下文，压缩后会遗漏更早的用户对话。现将聊天历史来源改为 SessionManager 当前分支的原始 message 记录，保留压缩前内容，同时不混入其他分支。
- 新消息在 message_start 分配独立 UUID 身份，后续克隆流片段、message_end 与 turn_end 共享该身份。在 SDK message_end 原生落盘之前附加 arcaneMessageKey，重开读取相同值；输入回执在用户消息进入上下文时绑定该身份。核对并测试安装的 SDK 原生监听与持久化顺序，未增加用户消息、模型调用或独立历史文件。
- 旧消息使用原生会话 entry ID。渲染器保留 role:timestamp 兼容定位，旧阅读锚点、展开状态、输入回执与未读分界仍可找到原内容；下一次保存阅读位置使用新身份。已有时间戳碰撞的旧锚点本来无法区分两条消息，兼容查找保持原先首个匹配语义，新身份不再增加该歧义。
- 新增 3 项测试：流式克隆与终稿同身份、下一条同时间消息不同身份；真实 SDK 原生压缩及分支切换的完整历史；真实 SDK 受控双轮生成、同时间戳回复不合并、输入回执匹配及磁盘重开身份一致。Electron 会话验收增加旧阅读锚点迁移，既有活动区验收继续通过。
- 完整历史正确恢复会增加长会话的传输及渲染数据量，不能据此宣称性能达标。下一阶段继续历史分页、缓存容量、P95 测量，以及完整系统入口验收；M8 整体仍未完成。

### M8c1 — 历史分页接口与分支索引 — 2026-09-07

- desktop 全量 397 项测试、类型与源码边界检查，以及 47 个 Markdown 文件链接检查通过。
- 新增 HistoryIndex，在原生分支上建立消息身份和工具结果索引，按稳定身份读取前页、后页或阅读锚点附近内容。工具结果匹配由逐条反向遍历历史改为一次顺序索引；重复工具 ID 只关联各自前面的调用，不回填给另一轮调用。
- 默认请求页为 100 条，上限 200；返回总条数、首尾身份和前后页标记。支持新消息身份、旧时间戳定位、思考块和工具卡片锚点。无效请求、互斥游标及离开当前分支的游标均明确报错。
- AgentHost 按原生 SessionManager 实例和 leaf ID 缓存索引；新消息、工具结果、压缩和分支切换导致重新建立索引。分页只转换和复制页内消息及其工具结果；调用者修改响应不能修改原始会话记录。完整历史入口复用同一逻辑。
- 主进程受信快照 IPC、preload 和类型声明支持可选 historyQuery。兼容调用暂保留完整历史，前端接入在 M8c2 继续；本提交不能作为“切换已经只传一页”或性能达标的证据。
- 新增 4 项测试覆盖一万条消息分页遍历无遗漏/重复、前后页边界、各类阅读锚点、无效请求、跨页工具结果、响应可变引用隔离、原生索引复用、追加结果更新和分支游标失效。原生 SDK 身份/输入验收和真实 Electron 会话、活动区验收继续通过。

### M8c2 — 对话历史窗口、翻页与原位置恢复 — 2026-09-07

- 会话打开、切模式、重载及同步快照默认返回最近 100 条历史。界面提供更早/后续消息入口，每次替换历史窗口；返回最新会主动取最新页。保存的位置不在初始页时按稳定身份请求附近窗口，不要求用户重新翻找。
- 旧页收到最新文字和工具事件时仍更新事件游标及真实任务状态，不把新正文拼接到旧历史中间；已显示的运行工具仍接收结果。浏览旧页或存在未恢复锚点时不能推进最新内容的已读位置。旧页不额外展开页外最新工具/流式内容。
- 页请求捕获会话、导航及请求身份，切换后的迟到响应不能覆盖新会话。失败保留当前页、草稿、附件和阅读锚点，提示可重试原请求；锚点不在当前分支时明确说明并展示最近消息。跨页保留已展开/收起的工具与思考状态。
- 真实 Electron 验收发现 smooth CSS 会让程序恢复锚点异步滑动，改为恢复时使用 instant 定位。前端刷新后恢复页位置，不触发模型调用；持续增长的最新视图在任务轮次结束且直接子节点超过 220 时重新取阅读窗口，减少多轮积累。
- 新增 `node apps/desktop/test/smoke-history.mjs`：真实 AgentHost、原生 SessionManager、HistoryIndex、preload、HTML 和 IndexedDB；1,200 条历史验证初次仅渲染 100 条、翻页、草稿/图片与展开状态、跨模式锚点恢复、旧页后台结果不抢滚动、返回最新、失败提示重试、刷新以及迟到页面拒绝。既有会话和活动区 Electron 验收继续通过。
- desktop 全量 397 项测试通过；随后补充跨页展开状态保留后，新的 Electron 长历史验收再次通过。类型、源码边界和 47 个 Markdown 文件链接检查通过。
- 边界：100 条是历史消息窗口，不是总字节或总 DOM 节点硬上限；单条大文本/多工具、当前任务快照及输入回执仍可能较大。前端跨会话缓存回收、最大并发下 P95、系统托盘与通知等实际行为仍待审计，不能据分页功能声称完整性能或 M8 达标。

### M8d — 前端缓存回收与事件缺口校准 — 2026-09-07

- 快照缓存按最近读取顺序保留最多 8 个会话，附加 8 MiB 的数据估算容量。事件正文每会话最多 512 条、最多 32 个会话，并按 16 MiB 的估算容量回收；估算遍历有工作量上限，单个过大的可重建缓存直接放弃保留。
- 事件正文回收后保留轻量 epoch/最高 seq 和退役身份。旧快照若落后于已观察事件，明确要求重新取快照，不能因正文缓存为空就断言没有增量；旧实例迟到事件仍被拒绝。新实例没有事件时的权威快照换代协议保持不变。
- WorkspaceStore 默认缓存 16 个会话、16 MiB 估算数据；只回收已经提交到 IndexedDB 的后台副本。当前工作区和未保存/写入失败的数据固定保留。旧事务完成不能解除新草稿的保留状态；旧读取跨过新写入且新副本已回收时重新读盘，不复活旧内容。草稿、图片、阅读位置及 outbox 数据仍按原事务保存。
- 切换会话时清理已交给工作区保存的内存回答草稿，返回后从工作区恢复；空 outbox 映射保留数量限制为 16，非空或正在发送的 outbox 不回收。删除继续清除缓存、脏记录和同步身份，保留删除屏障。
- 复查发现 session_switched 的嵌入快照游标原先不包含事件自身，恢复后可能再次回放自身。SessionProjection 现在把外层事件游标作为嵌入快照边界，原快照不被修改，后续事件正常衔接；新增原生 Electron 事件验证只安装一次。
- 新增 6 项回归测试覆盖缓存顺序/容量、事件正文回收后的缺口与退役实例、写入失败保留、旧写完成与新写失败竞争、旧读跨过写入/回收，以及切换快照不回放自身。全量 402 项测试通过；随后新增切换边界后，同步与缓存定向 20 项测试通过。
- Electron 长历史验收增加 40 个工作区的缓存压力：快照、事件正文及已保存工作区均被回收，随后从真实 IndexedDB 恢复草稿、图片和原阅读页。原会话/问答、活动区 Electron 验收和类型/源码边界检查继续通过。
- 边界：估算容量用于限制缓存正文，不是进程 RSS 承诺。未保存数据、非空 outbox 和防止事件/删除复活的轻量身份允许超过普通缓存数量，不能为了内存上限丢弃它们。最大并发、长工具输出和实际设备上的 P95/RSS 仍需测量；系统入口验收及完整规格审计仍未完成。

### M8e — 最大并发响应基准与历史回执分页 — 2026-09-07

- 新增可重复的隐藏 Electron 基准，16 个真实调度器活动任务，每会话 10,000 条历史及 5,000 条已消费输入回执；持续进度/任务状态更新，实际任务与活动日志持久化。测量同模式和跨模式各 60 次切换，区分缓存呈现与执行侧新快照校准，记录 CPU、系统、样本、P95 和各进程 working set。
- 基准发现所有历史回执正文仍随分页快照传递，会使大快照无法保留在缓存。现按页筛选已处理回执正文，保留未消费输入，同时返回全部已接收命令 ID 供 outbox 去重。没有删除原历史、日志或幂等身份。新增原生快照回归及真实 Electron 页外已接收输入验证，不再出现无必要的重试按钮。
- 最终独立运行通过：选中/内容 P95 31.5 ms，执行侧校准 62.9 ms，进度 60 ms，任务状态 103 ms；120 次切换全部命中缓存。完整方法、设备、适用边界和原始结果见 [性能基准](performance-baseline.md)。
- desktop 全量 404 项测试、类型及源码边界检查通过；Electron 长历史、会话/问答与活动区三组验收通过。
- 该结果证明约定负载与本机受控渲染基准，不能替代真实 production main、物理系统入口或其他平台验收。大附件/超大单条输出、系统托盘与通知、完整规格逐项审计仍需完成；整体目标保持未完成。

### M8f — 生产主进程、真实 SDK 与原生退出链路 — 2026-09-07

- 新增 `node apps/desktop/test/smoke-production-main.mjs`，直接加载生产 main，不替换 IPC handler、会话注册表、调度器或 SDK。真实 SDK 通过本地 HTTP/SSE 模型端点流式响应，两个请求可由测试独立完成；模型没有返回工具调用。
- 使用临时 userData、agent 目录、用户配置和 runtime 路径，清除继承的凭证环境变量，关闭遥测；不连接外部模型或 Foundry。运行前需准备 renderer assets 与 bundled Node runtime（分别使用 desktop 的 `prepare:renderer-assets`、`prepare:node-runtime` 脚本）。技能更新指向不可用 loopback，预期输出一次保留已有技能的警告。
- 验证同模式新建 B 不停止 A、A/B 实际同时流式运行、返回 A 只恢复一个流式节点、跨模式完成 A 不污染当前草稿、返回 A 立即显示终稿，以及真实页面 reload 保留 B 运行。整个过程模型请求严格为 A、B 各一次。
- 使用原生 BrowserWindow、Tray 和 Menu；测试提供关闭对话框的决策，验证取消保留运行、后台继续保留窗口和任务、托盘菜单/点击回调恢复窗口、退出停止真实 SDK 流。正常 will-quit 时断言 B 已停止且不再 busy、托盘销毁。runner 同时要求正常退出和成功标记，避免未执行断言就退出的假通过。
- 初次完整链路通过；增加运行中 A→B→A 往返及单一流式节点断言后再次通过。类型及源码边界检查通过。此阶段没有修改生产代码；404 项模块测试的最近全量结果仍为 M8e，不宣称本阶段重跑全量。
- 边界：窗口由测试保持隐藏；对话框选择及托盘事件由代码触发，不覆盖人工点击、物理显示、系统通知送达或其他操作系统。崩溃后完整恢复体验、真实 Foundry 世界操作和规格逐项审计仍需继续，整体目标未完成。

### M8g1 — 中断后的显式恢复入口 — 2026-09-07

- 规格审计发现已恢复的 interrupted 任务只有状态标签，没有可发现的恢复入口。现在状态栏说明保留已有记录且未自动重试，并提供“核对进展后继续”。点击把核对已有成果、避免直接重复已成功外部操作的要求加入当前草稿；用户审阅后通过原发送流程开始下一任务。
- 保留原草稿和附件，重复点击不重复加入要求。按钮捕获会话及任务身份，过期按钮不能修改其他会话。中断状态保留到用户实际开始下一任务；准备草稿本身不提交命令、不请求模型。中英文文案同步。
- 真实 Electron 会话验收覆盖中断入口、草稿/附件保留、重复点击、跨模式隔离、切回和刷新后恢复，以及请求次数不变。任务协调与日志压缩 17 项测试通过，覆盖持久化中断标记、未消费附件与不自动重放；类型及源码边界检查通过。
- 该入口帮助用户明确提出核对要求，不构成对外部副作用的事务去重保证。真实进程异常退出再启动的端到端验收仍未完成，继续作为 M8g 后续工作；整体规格未宣称通过。

### M8g2 — 首轮会话持久化与真实强制结束恢复 — 2026-09-07

- 新增 `node apps/desktop/test/smoke-crash-recovery.mjs`：启动隔离的生产 main，完成 A、保持 B 的第一轮真实 SDK 流运行；在明确就绪信号后由 runner 强制结束自己启动的子进程，等待它退出，再以同一 userData/agent/runtime 目录启动新进程。未调用正常停止或退出流程。
- 首次验收复现缺陷：A 文件存在，B 文件不存在；任务输入日志已登记，但重启时 B 无法导航。原因是 SDK 默认延迟到首条 assistant 消息结束才创建会话文件，前端恢复协议无法弥补缺失的会话身份。
- AgentHost 现在创建原生会话后先认领模式，以独占创建和 fsync 写入 header/模式记录，再通过 SDK 公开 open 接口进入追加模式。后续消息按原 SDK 写入，无需修改 SDK 私有持久化标志。新建空会话也有持久身份；创建失败不继续接收任务。
- 修复后真实强制结束/重启验收通过：A 完整结果和 completed 状态保留；B 原任务身份及已接受输入可恢复，显示 interrupted 和恢复入口；准备恢复草稿没有任何模型请求。增加实际发送继续要求后再次通过：同一 B 会话创建新任务，仅显式提交的 C 请求执行并完成，旧 A/B 没有重放。
- 新增原生 SDK 回归测试覆盖首轮完成前可列表/重开、模式身份保留、user-only 写入、首条 assistant 追加无文件冲突和重复 header。已有全量 404 项测试通过，新回归另行通过；最终类型/源码边界通过，原生产 main 并发及正常退出验收继续通过。
- 边界：验证的是本机 Windows 进程强制结束，不是断电或磁盘损坏；模型端点受控且没有执行外部工具。未落盘的流式片段不承诺跨进程保留，外部工具是否已经产生效果仍需核对，不能由“新任务执行一次”推导事务回滚或工具副作用去重。物理系统入口、真实 Foundry 操作及完整规格审计仍未完成。

### M8h — 规格逐项证据索引与缺口审计 — 2026-09-07

- 新增 [acceptance-audit.md](acceptance-audit.md)，覆盖 spec §§1–12 的对象、故事步骤、界面条款、状态规则、生命周期、10 项不变量、16 个验收场景及性能门槛；明确自动化、模块证据与平台实测的边界。
- 源码审查识别 G1–G5：发送入口未区分新任务/补充；停止期间提交会清空草稿再被后端拒绝；恢复失败任务不显示已保存 error；同步异常缺少最后更新时间；删除后替代会话启动失败时 controller 的已启动缓存无法代表 activeHost 是否存在。后续按审计列出的回归要求修复，不能以当前测试通过宣称条款完成。
- 生产联合场景和物理系统交互分别列为 G6/G7，避免重复把隐藏 Electron 或模拟通知描述成完整平台验收。文档状态与 architecture 的实施前调查标签同步更新；规格范围未削减。
- 本阶段为源代码/测试断言与记录审计，没有重跑产品测试。Markdown 链接及 diff 检查作为文档验收；全部缺口关闭并具备相应证据之前，整体目标保持未完成。

### M8i — 发送语义与停止期间草稿保护 — 2026-09-07

- 关闭审计 G1/G2。输入区可见文字及发送按钮可访问名称按当前会话任务状态显示“开始新任务”“补充当前任务”；不沿用上一会话的临时 busy 标记。最终输入归属仍由执行侧接收时决定。
- 停止请求按 sessionId/taskId 捕获，立即显示停止反馈并阻止按钮、Enter、直接提交及重试入口发送；草稿和附件保留且可编辑。后端 stopping 快照在刷新后继续保护。停止失败可见且可重试，失败提示仅属于原会话；终态或新任务消除旧请求，迟到回执不改变新任务操作状态。
- Electron 会话验收新增延迟回执、停止失败、跨模式隔离、草稿附件保留、刷新恢复 stopping、停止完成重新允许发送、旧回执不阻塞下一任务。实际验收暴露并修复了切换时发送标签短暂沿用旧 busy 的问题。
- 更新后会话、活动区、生产 main 三组 Electron 验收通过；补充重试入口保护后的会话验收再次通过，最终类型和源码边界检查通过。本阶段没有后端状态机变更，未重跑全量模块测试。
- G3–G7 和最终规格关闭仍待完成；新增文字的跨设备物理布局仍随平台视觉验收核对，不以隐藏页面自动化替代。

### M8j — 终态原因与下一步的统一恢复 — 2026-09-07

- 关闭审计 G3。失败、停止及取消的详情由 showTaskState 从同一 task 数据渲染，实时事件与恢复快照走同一路径；移除仅实时失败时追加的状态行，避免反复同步堆积错误。错误按纯文本保留换行，较长内容在有界区域内滚动，不解析为 HTML。
- 已提供 error 时展示原原因；缺失时明确说明执行侧未提供具体原因。取消注明任务未开始。失败和停止提供保留成果、核对后继续或新任务的下一步；“核对进展后继续”只加入草稿，保留附件，用户发送后才开始新执行，不自动重试外部操作。
- Electron 会话验收覆盖后台失败打开、重复事件/同步、刷新、原错误文本与 HTML 字样安全呈现、恢复草稿附件、停止的有/无原因、下一任务清除旧详情，以及零额外模型提交。活动区验收增加取消说明检查并通过；最终类型、源码边界检查通过。
- 未修改后端状态机，本阶段未重跑全量模块测试。真实工具失败/停止及系统视觉等联合场景仍按 G6/G7 验收，G4/G5 的实现缺口继续处理，整体目标未完成。

### M8k — 删除替代失败后的模式恢复与实例保留 — 2026-09-07

- 关闭审计 G5。组合回归先复现 controller 把已启动但 activeHost 已为空的注册表误报就绪。现在以当前 activeHost 判断可用；重试继续共享启动 Promise，启动返回但仍无选中实例时明确失败，不写入虚假就绪缓存。
- 生产 main 验收进一步发现：A 删除完成、替代启动失败后，初始化从磁盘重新创建了仍在后台运行的 B，导致原运行现场无法呈现。SessionRegistry.start 现在优先重新选中存活 resident，沿用原 SDK、任务及现场，不重复打开历史。
- 新增两项回归覆盖删除后的失败重试、并发初始化合并、已删除身份不复活、后台实例保留。生产 Electron fixture 使用真实删除/currentSession IPC，注入一次替代启动失败，验证随后返回的 B 与原 SDK host 是同一个实例、半截内容保留、模型请求不重复，最后能正常停止退出。
- 控制器/注册表定向 17 项测试、生产 main Electron 验收、类型与源码边界检查通过；desktop 全量 407 项测试通过。G4 和 G6/G7 仍需继续，整体目标未完成。

### M8l — 同步确认时间、超时恢复与静默检查 — 2026-09-07

- 关闭审计 G4。缓存安装标明等待执行侧校准，不能把渲染缓存当作一次新确认。新快照及连续实时事件更新本次页面生命周期内的最后确认时间；慢同步、历史读取失败与同步失败均显示该时间，没有确认则明确说明。语言切换同步更新提示。
- 快照只读请求十秒超时，释放重试入口；原 invoke 后续返回仍被忽略。同步请求按导航身份合并，允许用户离开又回来后发起新校准；旧请求的计时器、失败和 finally 清理不能覆盖新现场或解除新请求的门闩。同一次导航内维持单飞，防止安装过程中重入发起快照。
- 每五秒检查当前活动任务的静默时间；超过十五秒未收到确认且没有近期检查时读取快照。长工具或模型没有输出不等于结束：探测失败仅改变同步提示，保留最后任务状态、内容、草稿及附件，不提交任何模型工作。
- Electron 会话验收新增缓存确认时间、超过一秒提示、A→B→A 的迟到失败、失败重试、挂起请求十秒超时、超时后的迟到结果、静默探测与零额外模型提交。活动区和生产 main 验收通过，最终类型/源码边界及链接检查通过。
- 长历史验收中途曾在刷新后状态等待处超时，增加失败状态诊断；最终代码独立通过后又连续三次通过，验证历史页、草稿附件、锚点及缓存回收恢复。没有把该次超时计为通过，也未据此声称证明了所有时序组合。
- 本阶段无后端状态机变化，未重跑全量模块测试。新增静默探测的最终性能需在 G6/G7 集成稳定后复核；系统物理交互与生产联合场景仍待完成，整体目标保持未完成。

### M8m1 — 真实长工具、跨模式恢复与执行计时 — 2026-09-07

- 新增 Windows 验收 `node apps/desktop/test/smoke-production-tool.mjs`，复用生产 main/IPC/SDK fixture，受控模型调用真实 PowerShell 工具。工具在隔离临时目录写一次启动记录并等待释放文件；可明确证明工具已进入执行。失败清理释放该文件并等待本测试的任务停止，避免遗留等待进程。
- 首轮复现工具起点跳动：实时卡片使用 renderer 收到事件时的 Date.now，恢复快照使用主进程开始时间。SessionProjection 现在将统一 startedAt/finishedAt 同时发布到实时事件和快照；renderer 初始展示与完成耗时使用这些时间，旧格式事件保留兼容回退。
- 验证 B 的真实工具在跨模式、页面刷新后仍运行，起始时间完全一致；打开 A 点击停止后立刻切模式，只停止 A，B 继续等待。释放 B 后真实工具结果进入 SDK 上下文并生成终稿，启动记录只有一次、请求仅 A/B 初始各一次及 B 的工具结果续轮一次。
- 增加完成后刷新验收，确认结束耗时与执行侧起止时间一致且刷新后保持；投影回归增加实时/快照时间一致断言。最终长工具验收通过，投影 7 项定向测试、类型及源码边界通过，desktop 全量 407 项测试通过。
- 覆盖 G6 的长工具/跨模式/刷新与停止隔离子场景；实际模型/目录组合、自动重试、乱序、Foundry 世界操作和物理系统入口仍待继续，不能据 PowerShell 验收宣称全部工具/平台已通过。

### M8m2 — 生产模型/目录隔离与默认模型边界 — 2026-09-07

- 新增 `node apps/desktop/test/smoke-production-context.mjs`，使用生产目录/provider/模型/default IPC、真实 SDK 和 loopback 模型端点。目录对话框结果由 fixture 提供为临时工作区 A/B，分别核对 host 与原生 SessionManager 的目录；切目录创建新会话，A 执行不变。
- 复现规格偏差：settings:default-model 仍把默认模型应用到所有已有空会话，覆盖用户已经选定的模型。现在仅持久化新默认，由之后创建的会话采用；已有空会话与运行会话均保持当前选择。
- 保存 provider 凭证改为仅激活已经选择该 provider 的空闲会话，而不是给空会话分配全局默认。补充验证先选择缺密钥的非默认 provider，再补齐凭证后 SDK 实际模型正确激活；不改变正在运行的 A/B。
- 验证 A、B 不同模型实际发到 HTTP 请求，B 运行时变更模型显示 pending 且不改变本轮；下一任务才使用新模型，A 的模型和目录始终保持。严格只有 A/B/C 三次请求，分别使用 arcane-spark、model-b、model-next。已有空会话保留选择，新建会话取新默认。
- 生产模型/目录验收通过，类型及源码边界检查通过，desktop 全量 407 项测试通过。G6 的自动重试、乱序及实际世界操作、G7 物理平台交互与最终性能仍待继续，整体目标未完成。

### M8m3 — 自动重试阶段的显示、恢复与取消 — 2026-09-07

- 新增 `node apps/desktop/test/smoke-production-retry.mjs`，复用生产 main/IPC/renderer 和真实 SDK。隔离 settings 配置两次重试及八秒退避，loopback HTTP 返回受控 overloaded 错误，由 SDK 实际调度重试。
- 验收先复现执行侧已进入重试而界面缺少提示。新增中英文重试次数提示，实时事件与快照恢复共用展示逻辑。进一步修复 SDK 重试请求再次发出 agent_start 时投影提前清除 retry；现在保留至 auto_retry_end 或任务边界，不把重试启动误判为新任务。
- 验证 A 重试等待中切 B、跨模式返回、等待期间刷新、重试请求流式输出期间再次刷新，重试提示与原 taskId 均保留；成功后提示清除。C 退避时停止并切 B，超过退避时间仍只有一次 C 请求，B 持续运行并正常完成。A/B/C 实际请求次数严格为 2/1/1。
- 生产重试和普通会话 Electron 验收通过，投影定向七项测试通过，最终类型/源码边界检查通过。首次全量测试暴露历史恢复测试的 VM 环境缺少新函数，补齐并增加恢复重试次数断言后，全量 407 项通过；Markdown 链接及 diff 检查通过。
- G6 自动重试子场景完成；生产乱序导航、实际 Foundry 世界操作、G7 平台交互及最终性能/规格复核仍待完成，整体目标保持未完成。

### M8m4 — 生产乱序导航与快照/终态边界 — 2026-09-07

- 新增 `node apps/desktop/test/smoke-production-navigation.mjs`，使用生产侧栏、IPC 和真实 SDK，测试包装 IPC 注册仅截留真实 handler 已生成的回包。控制旧 B 打开结果在用户点回 A 后返回；两个模型请求持续运行，缓存和草稿来自真实 renderer/IndexedDB。
- 复现快速返回被忽略：点击闭包沿用列表生成时的 active，同时 sameModeContext 要求旧 sessionId 仍等于当前选择。缓存已切到 B 时，旧列表的 A 点击会被拒绝。导航改为使用当前 selectedSessionId 判断，保留模式及 generation 校验，在点击时捕获请求上下文；删除等其他操作的校验不变。
- 验证 A→B 缓存→A 的真实侧栏点击、旧 B 回包释放后前端和主进程均仍选中 A、A/B 草稿独立且内容不串。另截留 A 的 running 快照，等真实 SDK 完成且页面显示终稿后才返回，等待同步 Promise 完整结束，确认完成状态不回退、终稿只有一份、草稿保留。
- B 随后正常完成；实际模型请求严格为 A/B 各一次。生产导航验收、普通会话 Electron 回归、渲染协议/注册表 24 项定向测试及类型/源码边界检查通过；本阶段未重跑全量模块测试，未改执行状态机。链接及 diff 检查通过。
- G6 乱序导航/终态边界子场景完成。实际 Foundry 世界操作、G7 系统交互及最终性能/规格复核仍待完成，整体目标未完成。

### M8n1 — Windows 原生关闭与最小化验收 — 2026-09-07

- 新增交互入口 `node apps/desktop/test/review-native-lifecycle.mjs`，生产 main/IPC/renderer/SDK 与原生对话框、托盘均保持真实；隔离数据和 loopback 模型。该入口需要实际 UI 操作，不能计作自动 smoke。
- 使用 Windows Computer Use 选中测试窗口，实际点击关闭→取消、最小化→恢复、再次关闭→停止任务并退出。最小化/恢复均记录 busy=true，半截回复和任务状态保留；最终 SDK 取消并落 stopped、busy=false、托盘销毁，只有一次模型请求，runner exit 0，原生窗口消失。
- 记录窗口和原生对话框观察、入口启动可见性修正及索引失效后的重新截图，见 [Windows 原生验收](windows-native-acceptance.md)。未把初次不可见启动或失败的索引点击算作成功；产品生命周期代码没有修改。
- G7 仅关闭上述 Windows 子场景。系统托盘实际点击、通知、后台完成焦点、多尺寸布局仍待验收。另发现会话列表首轮标题/数量滞后，登记 G9；实际 Foundry 安装已定位但未启动/修改世界，G6 该部分仍待完成。
- 共享 fixture 修改后，原有 `smoke-production-main.mjs` 回归通过；50 个 Markdown 文件链接检查及 diff 检查通过。仅新增测试入口和文档，本阶段未重跑全量模块测试。

### M8n2 — 首轮会话元数据与列表位置稳定 — 2026-09-07

- 关闭原生验收发现的 G9。SessionRegistry 对驻留实例使用原生 SessionManager.getEntries 的消息日志生成首条文字和计数，保留会话名称及原修改时间，不再沿用首轮尚未刷盘的 no messages/0 条摘要。日志包含所有分支，不能用压缩后的当前上下文代替总消息数。
- renderer 在输入 consumed、完整消息及 agent_end 时合并一次元数据读取，读取单飞；流片不触发读取。只更新现有行的标题与元信息，不替换节点或重排；抽屉打开时的 agent_end 也不再重建整个列表。迟到数据受模式/generation 及列表刷新版本约束。
- 新增生产 metadata runner，验证 A/B 首轮未完成时标题和计数正确、后台 A 完成更新为 2 条、当前 B 完成更新为 2 条；保存并逐项比较原 DOM 节点，确认可见抽屉在输入及前后台结束后身份与顺序不变。真实 SDK 请求严格 A/B 各一次。
- 注册表回归覆盖内存日志优先、跨分支计数和原修改时间不变。生产 metadata、普通会话 Electron 回归、类型/源码边界检查通过，全量 408 项测试通过；Markdown 链接及 diff 检查通过。
- 实际 Foundry、G7 剩余系统交互/多尺寸布局及最终性能/规格复核仍待完成；合并元数据读取的最终性能随全体验收复核，整体目标未完成。

### M8m5 — 实际 Foundry 世界写入与资源等待取消 — 2026-09-07

- 关闭 G6。使用本机已授权 Foundry 13.351、Node 22.23.2，将 test001/dnd5e 5.3.3 复制到独立临时数据目录，端口 30219；未修改原 E2E 或用户世界。许可证正常校验，测试 GM 登录后验收自动运行。
- 新增 `review-production-foundry.mjs`，生产 main/IPC/renderer/真实 SDK/browser_evaluate 执行完整调用链。A 在页面内的可控 Promise 前持有资源，B 调用同一资源后进入 waiting_resource，页面显示等待；停止 B 后其代码没有执行，A 继续持有执行现场。
- 返回 A 释放等待，公开 JournalEntry.create 创建唯一带随机 probe 标记的文档，工具结果回到 SDK 并产生终稿。查询验证恰好一个文档，随后用 Document.delete 清理；服务端日志确认同一文档创建/删除。模型调用严格 A/B/A，runner exit 0。
- 本次测试进程已正常退出，独立 Foundry 服务也已结束；副本保留在临时目录供诊断。环境、复现与范围见 [实际 Foundry 验收](foundry-resource-acceptance.md)。不据此宣称所有系统/模块和平台均通过；G7 与最终性能/规格审计继续。
- 共享 production fixture 的 metadata 回归、类型/源码边界检查通过，51 个 Markdown 文件链接检查通过。本阶段新增集成测试与记录，不修改资源协调器或工具执行代码。

### M8n3 — 连续消息与元数据负载下的性能复核 — 2026-09-07

- 补强 benchmark：列表 IPC 调用生产 SessionRegistry，计算每模式八个驻留会话的一万条原生日志元数据；每秒额外完成 16 条消息，触发 renderer 合并元数据更新。冷磁盘发现仍为缓存描述，方法和范围明确记录。
- 基准复现自动跟随丢失：scrollTop 受 CSS 平滑滚动影响，中途位置被误判为用户阅读旧历史，followLatest=false 并恢复到旧页。scrollToEnd 改为显式即时滚动；用户主动阅读仍由原监听器和工作区锚点管理。
- 完成消息测量允许使用同身份最终节点，同时增加跟随最新及无后续历史页的完成断言。最终单独运行通过：选中/内容 P95 37.6 ms，校准 68.2 ms，进度 71 ms，状态 133 ms；120 次切换全部缓存命中，两类导航各 60 样本均达标。285 次生产元数据计算 P95 18.44 ms。
- 长历史、普通会话和生产 metadata Electron 回归通过，类型/源码边界通过。普通会话测试的模拟阅读改为即时到位，并先断言确实离开最新位置；不依赖动画中途状态。没有后端状态机改动，本阶段未重跑全量模块测试。
- [性能复核及原始数据](performance-with-metadata.md) 保留方法、失败原因、最终结果和限制；旧 M8e 数据继续保留。G7 剩余系统交互及 G8 最终规格/文档复核仍待完成，整体目标未完成。

### M8n4 — 多尺寸渲染布局证据 — 2026-09-07

- 新增独立布局 runner，覆盖 1080×640、1520×920 和聊天宽度 456/320px 的分屏。使用生产 renderer 和 ActivityCenter，受控任务保持运行及输入草稿。
- 检查活动、任务状态、输入框及停止/发送控件边界，等待侧栏实际动画收起后截图；四张截图均完成目视核对。初次动画中途截图和空活动响应已修正测试夹具，产品代码未改动。
- [截图、几何数据及方法限制](layout-evidence/README.md) 明确区分渲染视口与物理窗口/Foundry 覆盖层。普通会话 Electron 回归及 Markdown 链接、diff 检查通过。
- G7 系统托盘、通知真实点击仍待验收，G8 最终审计未完成；本阶段没有宣称整体目标完成。

### M8o1 — 当前证据索引复核 — 2026-09-07

- 审计索引基线更新至 M8n4，移除开头相互矛盾的历史状态串，保留里程碑作为历史依据。将已验证的输入/停止/终态/同步、重试、长工具及导航条目改为对应当前证据，并补入 E19 布局证据。
- G6 的关闭不覆盖原表剩余的行为集成：单工具失败后继续、慢停止说明、停止保留实际产物、生产停止并删除运行会话。汇总为 G10，保留原规格范围，未将这些项目计为通过。
- README 默认模型说明按 E14 的生产断言修正为仅新会话生效。Markdown 链接及 diff 检查通过；仅文档变更，不重跑执行测试。
- G7、G10 与 G8 最终复核仍未完成；原生交互测试入口尚未验收，不随此文档提交。


### M8o2 — 生产运行会话停止并删除 — 2026-09-07

- 新增生产删除场景及 runner，通过实际 renderer 删除按钮、IPC/registry 和真实 SDK。确认框选择受控，断言文案与该会话标题一致。
- 暂缓 SDK 取消入口，验证执行侧 stopping、实例和文件保留、列表可追踪且删除禁用；释放后调用真实取消，停止确认后移除 A。B 一直流式运行并完成，模型请求严格 A/B 各一次。
- 最终删除 runner 与普通 production main 回归通过。仅测试及记录变更，未修改产品状态机；Markdown 链接及 diff 检查通过。
- G10 删除子项已关闭，单工具失败继续、真实慢停止和产物保留仍待验收。G7 系统交互、G8 最终复核继续。


### M8o3 — 实际工具失败恢复与停止保留成果 — 2026-09-07

- 新增 production tool recovery runner。真实 PowerShell 先输出受控错误并 exit 1；SDK 下一请求实际携带失败结果，页面工具卡失败而任务仍运行。
- 同一 taskId 再执行 PowerShell 写入隔离文件，结果进入 SDK 后停止任务。文件内容在停止和刷新后保持，两张工具卡分别恢复为失败/成功。A 全程继续并完成；HTTP 请求严格 A/B/B/B。
- runner 通过；仅测试和文档变更，未改产品状态机。Markdown 链接及 diff 检查通过。G10 剩余真实慢停止说明，G7 系统交互与 G8 最终复核继续。
