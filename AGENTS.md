# Agent rules — V14 升级期双栈隔离纪律

适用期:Foundry V14 升级线开发期间(方案见 `apps/desktop/docs/v14-upgrade-plan.md`)。本文是纪律层;
构造性隔离(marker 断言、双向 generation 门禁)是该方案 M0/M1 的代码工单,纪律不能替代它们。

## 受保护资产(任何操作不得影响)

- `D:\FVTT_DATA` —— F13 生产数据目录(COS 世界),只允许 F13 二进制(`foundryvtt-13.351-rebuild` + Node 22)访问。
- 端口 30000 / 30002 与 CDP 9230 上的实例 —— F13 生产/开发实例,不接管、不重启、不写世界;写操作只走 fvtt-cli / desktop 工具面。
- QA farm slot 0(30000)—— 双栈期冻结,永不进 farm 调度。

## 硬规则

1. **数据目录隔离**:v14 实例只允许指向独立 v14 数据目录(如 `D:\FVTT_DATA_V14`)。启动任何 v14 实例前,先确认目标数据目录存在 `.arcane-stack-v14` 标记文件;缺失即中止并报告,绝不"顺手补建后继续"。该检查落成入口脚本断言之前,由 agent 手动执行同等检查。
2. **单向迁移红线**:世界被 14.x 打开过即永久不可回 F13。任何会"打开世界"的操作,先核对实例代际与世界的 coreVersion;迁移类操作必须已有 `.arcane-world-backups` 备份且经人工确认。
3. **金本位副本纪律**:F13 原版世界从不注册进 v14 实例。v14 验证一律使用金本位快照复制出的副本,命名带 `-v14qa` 后缀,用后即弃,不在副本上做增量长期测试;金本位快照本身只读。
4. **端口带隔离**:F13 = 30000/30002/9230;v14 本机实例用 31000+ 段与独立 CDP 段;farm v14 槽用独立段。任何实例启动前核对端口不在对方带内。
5. **mod 安装代际核对**:向数据目录安装系统/模块前核对代际——dnd5e 6.x 与 14.x 模块禁入 F13 数据目录;F13 兼容线版本禁入 v14 目录(除非明确标注为对照实验)。
6. **改动范围**:v14 线改动只落本仓分支;`arcanedesk-fvtt-mods-private` 在 v14 MVP 期间不碰(auto2014 属 M3/M4,启动另有前提)。
7. **验收环境规则**:v14 验收一律在副本世界 + QA farm v14 槽进行;rules version 显式钉 2014;测试产生的文档用后清理(E18 模式)。

违反任何一条而拿不准时,停下来报告,不要尝试自行恢复。
